Tighten MalinGu's voice and give the whole app a modern-morphism feel.

## Copy
- Cut landing, FAQ and meta copy to short lines — one idea per sentence.
- Rewrite CTAs as first-person outcomes: "Start free", "See how it works", "Open my dashboard", "Create my account".

## Morphism
- New tokens in `src/styles.css`: `glass-morph` (blur + saturate glass surface with lift-and-glow hover) and `morph-in` / `morph-in-delayed` entrance animation.
- `fintech-card` upgraded with backdrop blur and a calm hover lift, so every existing card inherits it app-wide.
- Landing header, hero, feature cards and FAQ items use the new glass surfaces.
- Motion stays calm; everything is disabled under `prefers-reduced-motion`.